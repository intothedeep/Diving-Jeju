$(document).ready(function() {
    $('#summernote').summernote({
  	  height: 300,
	  minHeight: 200,
	  maxHeight: null,
	  focus: true
    });
    
    $('#modifySummernote').summernote({
		height: 300,
		minHeight: 200,
		maxHeight: null,
		focus: true
    });
    
    $('#replySummernote').summernote({
		height: 300,
		minHeight: 200,
		maxHeight: null,
		focus: true
    });
});